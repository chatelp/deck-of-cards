{
  "name": "deck-of-cards-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Modernized, cross-platform Deck of Cards monorepo",
  "packageManager": "pnpm@9.6.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@types/react": "^18.2.37",
    "@types/react-dom": "^18.2.18",
    "turbo": "^2.1.0",
    "typescript": "^5.5.4"
  }
}