// Simple build marker to verify the loaded code in Expo/Metro
// Update automatically at build time by embedding a timestamp.
export const RN_DECK_VERSION: string = `@deck/rn build ${new Date().toISOString()}`;
